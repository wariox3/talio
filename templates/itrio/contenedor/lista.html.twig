{% extends 'base.html.twig' %}
{% block content %}
    {{ form_start(form) }}
    <div class="container-fluid px-4">
        <h1 class="mt-4">Itrio</h1>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a href="{{ path('inicio') }}">Contenedores</a></li>
            <li class="breadcrumb-item active">Lista</li>
        </ol>
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-table me-1"></i>
                Contenedores
            </div>

            <div class="card-body">
                <div class="d-flex justify-content-between mb-3">
                    <div class="d-flex align-items-center">
                        <label for="" class="me-2"></label>
                    </div>
                    <div class="d-flex align-items-center">
                        <label for="" class="me-2"></label>
                    </div>
                    <div>
                        {{ form_widget(form.btnFiltrar, { 'attr': {'class': 'btn btn-sm btn-primary'} }) }}
                    </div>
                </div>
                <table class="table">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Subdominio</th>
                        <th>Plan_id</th>
                        <th>Plan</th>
                        <th>Usuario_id</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for contenedor in contenedores %}
                        <tr>
                            <td>{{ contenedor.id }}</td>
                            <td>{{ contenedor.nombre }}</td>
                            <td>{{ contenedor.subdominio }}</td>
                            <td>{{ contenedor.plan_id }}</td>
                            <td>{{ contenedor.plan_nombre }}</td>
                            <td>{{ contenedor.usuario_id }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
